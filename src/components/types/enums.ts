/* eslint-disable no-unused-vars */
export enum CheckID {
  category = '#category',
  country = '#country',
  language = '#language',
  name = '#name',
}
export enum CheckCategoryName {
  category = 'category',
  country = 'country',
  language = 'language',
  name = 'name',
}
